import { GLSPDiagramWidget } from '@eclipse-glsp/vscode-integration-webview';
import { injectable } from 'inversify';

@injectable()
export class PlantUmlGLSPDiagramWidget extends GLSPDiagramWidget {
    
}